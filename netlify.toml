[build]
  command = "npm run build && echo '/*    /index.html   200' > dist/_redirects"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  autoLaunch = false
